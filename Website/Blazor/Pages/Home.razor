@page "/"
<PageTitle>BugTracker - Home Page</PageTitle>
@code{
    protected override void OnAfterRender(bool firstRender)
    {
        if(!string.IsNullOrWhiteSpace(Config.Instance.MainRedirectTo) && firstRender) Navigation.NavigateTo(Config.Instance.MainRedirectTo);
    }
}

